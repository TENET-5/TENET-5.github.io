node_modules
.venv*
__pycache__
*.pyc
*.pyo
*.dll
*.so
*.whl
dist/
*.egg-info/
data/lobbying/*.zip
data/lobbying/*.csv
data/contributions/*.zip
data/contributions/PoliticalFinance/

# Protected TENET-5 OS & LIRIL Matrix Artifacts
*.db
*.sqlite
.liril_training.json
.liril_discoveries.db
.SATOR_ledger

# Backend tooling — not for public repo (contains TENET5 IP)
tools/
scripts/*
# Cap#297a: quantum-gate proof firer (committed)
!scripts/liril_quantum_proof.py
data/scrapers/
data/scraper_logs/

# EMH audit chain (proprietary hash tracking)
**/_emh_audit_chain.jsonl

# Proprietary TENET5 telemetry/agent data
data/nemoclaw_*
data/liril_*
# Cap#297a: per-cap quantum-gate proof ledger (committed)
!data/liril_quantum_proof.jsonl
# Public home-guide news presentation (safe — no internals)
!data/liril_news_presentation.json
!data/liril_news_articles.json
!data/liril_reporter_persona.json
!data/liril_station_playlist.json
# Page-matched VO packs for dock Guide me (public, no internals)
!data/liril_page_voice/
!data/liril_page_voice/**
# Outlet desk / substack catalogs are rebuilt into public HTML; keep JSON local unless needed
# (newsletter.html and press-wire.html are the public surfaces)

data/millennial_falcon_*
data/slate_manifest.json

# Compiled binaries
LirilClaw*.zip


.github/workflows/

# Archive output (build-archive.py generates these; too large for git)
tenet5-evidence-archive.zip
tenet5-evidence-archive.sha256

# Cap#260a — additions for file adoption batch (2026-04-28)
# Heavy media — defer until LFS or CDN decision
audio/
# Ship desk neural VO + captions (product path — not bulk archive)
!audio/desk/
!audio/desk/**
!audio/docs/
!audio/docs/**
assets/sprites/
assets/video/

# Local-only scratch / scripts
_check.ps1
check_banned.py
audit_log.txt
audit_raw.json
_tmp_untracked.txt
.cached/
.githooks/
var/

# LIRIL local telemetry dotfiles (data/liril_* pattern misses the leading dot)
data/.liril_*

# IDE-local directive surface
.vscode/liril_directive.md

# Cap#528: internal architecture docs (LIRIL stack, S.L.A.T.E paths, quantum-gate mandate) — not for public repo
.github/copilot-instructions.md
.vscode/instructions/

# Cap#260b-alpha — heavy raw-feed data subtrees (>50MB files; needs LFS or external store)
# Daily snapshots of full lobbying registry (each 80MB-2.2GB) and contributions audit CSVs
data/lobbying/
data/contributions/
data/network_analysis/influence_network.json

# Cap#264 JSON audit artifacts (regenerable via tools/cap264_json_audit.py)
data/json_audit_findings.jsonl
data/json_audit_summary.json

# Cap#306: runtime telemetry rewritten by stats_agent / live watchers (kept on disk, never committed)
data/status.json
data/canada_vigil_live.json
data/walkthrough_e2e_results.json
data/evidence_permalinks.json
data/narration_index.json

# Cap#312: post-build last-updated stamper (allowed exceptions to tools/ + workflows/)
!tools/build_last_updated_stamps.py
!.github/workflows/last-updated-stamps.yml

data/scraper_logs/
data/gc_sync/
data/prism_*
data/.prism_*

# targeting/campaign apparatus — never publish (legal compliance, Daniel 2026-07-11)
data/mp_email_directory.json
data/campaigns_to_send/
data/grover_target_campaigns.json
data/grover_target_campaigns*.json

# parliament-watch raw outputs — quarantined, curate before publish
data/parliament_brief_*.json
data/parliament_watch_trace.jsonl

# generated film b-roll source (large frame dumps stay in ComfyUI_native, not here)

# LIRIL Substack draft outbox (paste/bridge — not required on Pages)
data/substack_outbox/

